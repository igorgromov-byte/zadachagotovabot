services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./secrets:/app/secrets:ro
      - ./config.json:/app/config.json:ro
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
